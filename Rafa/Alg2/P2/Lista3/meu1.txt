funcao stringMinuscula(s:string):string
var
  procedimento deixaMinusculo(n:inteiro)
  inicio
    se n > 0 entao
      se s[n] >= 'A' e s[n] <= 'Z' entao
        s[n] <- chr(asc(s[n]) + asc('a') - asc('A'))
      fimse
      deixaMinusuculo(n-1)
    fimse
  fim procedimento
inicio
  deixa_minusculo(tamanho(s))
  retorne s
fim funcao
