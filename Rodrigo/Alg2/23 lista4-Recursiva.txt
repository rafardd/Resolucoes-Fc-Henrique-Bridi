tipo 
    PNo : ponteiro para TNo
    TNo : registro
            info : inteiro
            prox : PNo
        fimregistro 

funcao removeTodosNosMaiorIgualX(ref head : PNo; x : inteiro) : logico
var

    funcao removeTodosNosMaiorIgualXInterna(ref head : PNo; x : inteiro; removeu : logico) : logico
    var
        aux : PNo
    inicio
        se head = NULO entao
            retorne removeu
        fimse
        se head^.info >= x entao
            aux <- head
            head <- aux^.prox
            desaloca(aux)
            retorne removeTodosNosMaiorIgualXInterna(head, x, verdadeiro)
        fimse
        retorne removeTodosNosMaiorIgualXInterna(head^.prox, x, removeu)
    fimfuncao
inicio
    retorne removeTodosNosMaiorIgualX(head, x, falso)
fimfuncao



