tipo 
    PNo = ponteiro para TNo
    TNo = registro
            info : real
            prox : PNo
        fimregistro

funcao elementoApareceNalista(head : PNo; x : real) : logico
var 
    aparece : logico
inicio
    aparece <- falso
    enquanto head != NULO e nao aparece faca     
        aparece <- aparece ou (head^.info = x)
        head <- head^.prox
    fimenquanto
    retorne aparece
fimfuncao

Outra alternativa, buscando o conceito de busca simples (o teste pela posição, no caso de ter "passado" é justamente o ponteiro ter ficado nulo, pois, para olhar
o último elemento, estamos usando o ponteiro do penúltimo, que ainda não é nulo e, se o último elemento for != de x, head = NULO) : 

tipo 
    PNo = ponteiro para TNo
    TNo = registro
            info : real
            prox : PNo
        fimregistro

funcao elementoApareceNalista(head : PNo; x : real) : logico
var 
inicio
    enquanto head != NULO e head^.info != x faca    
        head <- head^.prox
    fimenquanto
    se head = NULO entao
        retorne falso
    senao
        retorne verdadeiro
    fimse
fimfuncao