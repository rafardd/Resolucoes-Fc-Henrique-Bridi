3 Lista 5

funcao duplicaSequenciaNoFinal(nome : string) : logico
var
  x : string
  f, g : sequencia de string
inicio
  se cria(g, "novo.txt") e abre(f, nome) entao
    enquanto nao fds(f) faca 
      leia(f, x)
      escreva(g, x)
    fimenquanto
    reinicia(g)
    enquanto nao fds(g) faca
      leia(g, x)
      escreva(f, x)
    fimenquanto
    fecha(g)
    fecha(f)
    apaga("novo.txt")
    retorne verdadeiro
  senao
    retorne falso
  fimse
fimfuncao


    
