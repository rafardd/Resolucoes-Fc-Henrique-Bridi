função substituiString(s, a, b: string): inteiro
var
  total : inteiro
  strAtual : string
  f : sequência de string
início
  total <- 0
  se abre(f, s) então
    enquanto não fds(f) faça
        leia(f, strAtual)
        se strAtual = a então
          escreva(f, b)
          total <- total + 1
        fimse
      fimenquanto
      fecha(f)
      retorne total
    senão
      fecha(foriginal)
      retorne -99999
    fimse
  senão
    retorne -99999
  fimse
fimfunção
      
          
