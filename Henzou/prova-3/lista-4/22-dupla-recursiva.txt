tipos 
  PNo = ponteiro para TNo
  TNo = registro
          info : string
          prev : PNo
          prox : PNo
        fimregistro

função removeValor(ref l: PNo; x: string): lógico
var
  temp : PNo
início
  se l = NULO então
    retorne FALSO
  fimse

  se l^.info = x então
    temp <- l
    l <- temp^.prox
    se l != NULO então
      l^.prev <- temp^.prev
    fimse
    desaloca(temp)
    retorne VERDADEIRO
  fimse

  retorne removeValor(l^.prox, x)
fimfunção
    
