const
  MAX = 100

tipos
  vetStr = vetor[1..MAX] de string

função retorneMenorStr(vet : vetStr, n : inteiro): string
var
  função encontraMenorStr(i : inteiro): string
  var
    menor : string
  início
    se i = 1 então
      retorne vet[1]
    senão
      menor <- encontraMenorStr(n-1)

      se v[i] < menor então
        retorne v[i]
      senão
        retorne menor
      fimse
    fimse
  fimfunção

início 
  se n <= 0 ou n > MAX então
    pare
  senão
    retorne encontraMenorStr(n)
  fimse

fimfunção     

   
  
