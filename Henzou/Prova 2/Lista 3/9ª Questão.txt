função retornaDecEmHex(n : inteiro): string
var
  função converteDecEmHex(n : inteiro): string
  início
    se n = 0 então
      retorne ""
    senão
      se n mod 16 <= 9 então
        retorne converteDecEmHex(n div 16) + chr(asc('0') + (n mod 16))
      senão
        retorne converteDecEmHex(n div 16) + chr(asc('A') + (n mod 16) - 10)
      fimse
    fimse
  fimfunção

início
  se n = 0 então
    retorne "0"
  senão
    se n < 0 então
      retorne "-" + converteDecEmHex(-n)
    senão
      retorne converteDecEmHex(n)
    fimse
  fimse
fimfunção
