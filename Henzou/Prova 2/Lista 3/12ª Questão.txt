função converteStrOctEmDec(s : string): inteiro
var
  função confereStrOct(i : inteiro): lógico
  início
    se i = 1 então
      se (s[i] = '-' e tamanho(s) > 1) ou (s[i] >= '0' e s[i] <= '7') então
        retorne VERDADEIRO
      senão
        retorne FALSO
      fimse
    senão
      se s[i] < '0' ou s[i] > '7' então
        retorne FALSO
      senão
        retorne confereStrOct(i - 1)
      fimse
    fimse
  fimfunção

  função converte(n, fator : inteiro): inteiro
  início
    se n = 0 ou (n = 1 e s[n] = '-')  então
        retorne 0
    senão
      retorne (asc(s[n]) - asc('0')) * fator + converteStrOctEmInt(n - 1, fator * 8)
    fimse
  fimfunção 

início
  se tamanho(s) = 0 então
    pare
  fimse

  se confereStrOct(tamanho(s)) então
    se s[1] = '-' então
      retorne converteIntOctEmDec(converteStrOctEmInt(tamanho(s)), 1) * -1
    senão
      retorne converteIntOctEmDec(converteStrOctEmInt(tamanho(s)), 1)
    fimse
  senão
    pare
  fimse
fimfunção
