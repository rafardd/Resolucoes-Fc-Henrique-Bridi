TIPOS
    PNo = PONTEIRO PARA TNo
    TNo = 
        REGISTRO
            info : real
            prox : PNo
        FIM REGISTRO

PROCEDIMENTO os_ultimos_serao_os_primeiros(REF l:PNo)
VAR
    ant, ult: PNo
INICIO
    SE l != NULO E l^.prox != NULO ENTAO
        ant <- l
        ult <- l^.prox
        ENQUANTO ult^.prox != NULO FAÇA
            ant <- ult
            ult <- ult^.prox
        FIM ENQUANTO
        ant^.prox <- ult^.prox
        ult^.prox <- l
        l <- ult
    FIM SE
FIM PROCEDIMENTO