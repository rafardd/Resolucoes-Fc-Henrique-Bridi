algoritmo GerarNumerosAleatorios
var
    a, b, valor_aleatorio: real
    aux: inteiro
inicio
    escreva("Insira um número real: ")
    leia(a)
    aux <- a * 10
    se aux = trunca(aux) entao
        se a != trunca(a) entao
            escreva("Insira um número real menor: ")
            leia(b)
            aux <- b * 10
            se aux = trunca(aux) entao
                se b != trunca(b) entao
                    se a > b entao
                        escreva("Não foi possivel realizar a operação.")
                    senao
                        valor_aleatorio <- (trunca((b+a)*100 + 1) * aleatorio()) mod trunca((b-a) * 100 + 1) / 100 + a
                senao
                    escreva("Não foi possivel realizar a operação")
                fim se
            senao
                escreva("Não foi possivel realizar a operação")
            fim se 
        senao
            escreva("Não foi possivel realizar a operação")
        fim se
    senao
        escreva("Não foi possivel realizar a operação")
    fim se
fim algoritmo